# gpt-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gpt-core` to build the library.

## Running unit tests

Run `nx test gpt-core` to execute the unit tests via [Vitest](https://vitest.dev/).
